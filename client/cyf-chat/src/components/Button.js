import React from "react";

const Button = () => {
  return (
    <div className="btn">
      <button>Latest Messages</button>
      <div>
        <button>All Messages</button>
      </div>
    </div>
  );
};

export default Button;
